export { dbConf } from './database';
export { swaggerConfig } from './swagger';
export { jwtConfig } from './jwt';
export { multerOptions, multerConfig } from './multer';

export const tmpFolder = 'dist/tmp';
export const fileFolder = 'dist/files';
